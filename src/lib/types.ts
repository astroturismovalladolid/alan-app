export interface LightPollutionImage {
  id: string;
  url: string;
  dataAiHint: string;
  location: string;
  rating: number; // 1 to 5
  description: string;
  author: string;
  timestamp: string;
}
